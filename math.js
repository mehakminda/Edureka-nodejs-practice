module.exports.add = function addition(x,y){ 
    console.log("Addition of "+x+" add "+y+" is "+(x+y));
    //return a+b;
}

module.exports.sub =function substrction(x,y){ //f name could be annonymous too since it already has a name sub
    console.log("Substraction of "+x+" add "+y+" is "+(x-y));
}